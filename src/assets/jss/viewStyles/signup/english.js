import { makeStyles } from "@material-ui/core";
//import { responsiveStyles } from "../../common/responsiveStyles"
import { signupMobCommon } from "./signupMobCommon";

export const signupEngMobile = makeStyles((theme) => ({
     ...signupMobCommon,
     
}));