import React from "react";
import Routes from "../routes";

const Layout = () => {
  return (
    <div>
      {/* Nav bar */}
      <Routes />
      {/* Footer */}
    </div>
  );
};

export default Layout;
